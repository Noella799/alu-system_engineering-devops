# Web Server Project

This project is part of the ALU System Engineering DevOps track.

It contains Bash scripts for managing web servers.

**Task 0:** `0-transfer_file`  
This Bash script transfers a file from the client to a server using SCP.  
It accepts four parameters: the file path, the server IP, the username, and the path to the SSH private key.

